document.addEventListener('DOMContentLoaded', () => {
    const btn = document.getElementById('mainBtn');
    btn.addEventListener('click', () => {
        alert('СИСТЕМА METAMPERIYA АКТИВИРОВАНА!');
    });
});